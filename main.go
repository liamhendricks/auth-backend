package main

import (
	"fmt"
	//"path/to/your/cmd/package" cmd
	"os"
)

func main() {
	//update cmd import to your path
	//if err := cmd.RootCommand.Execute(); err != nil {
	//	fmt.Println(err)
	//	os.Exit(-1)
	//}
}
